#Funciones en spark


# count() - Regresa el número de elementos de un RDD
#collect() - Regresa todos los elementos de un RDD.
#foreach(f) - regresa aquellos elementos que cumplen con la condición dentro de la función foreach